@inject ICategoryService CategoryService

<MudNavMenu>
    @foreach (var category in CategoryService.Categories)
    {
        <MudNavLink Match="NavLinkMatch.All" Href="@category.Url">@category.Name</MudNavLink>
    }
</MudNavMenu>
